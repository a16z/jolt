# Commit resource-pressure campaign

Started 2026-09-06 04:52 UTC after the user cleared the earlier goal and
authorized recentering on the dominant commit panel. The earlier commit/eval
campaign is superseded, not resumed. Its observations remain immutable.

## Goal and authority

First milestone: >=20% higher useful throughput in the P19/D128/rank-3 root
commit panel, and >=1.0 s verified Fibonacci T28 end-to-end saving against
the accepted parent. At the recorded 12.319893832 s panel this corresponds
to <=10.266578193 s, saving2.053315639 s. Twenty percent is a milestone,
not a ceiling: user explicitly authorized pursuing larger supported gains.
Continue through bounded epochs while a priced, evidence-backed lever
remains. Projected M5 10 MHz average is a separate, unvalidated-hardware
north star; never equate it with measured M4 throughput.

One M4 Max, serial, no subagents, no paid/remote GPU resources, no pushes,
no Xcode/admin installation. User-space read-only telemetry is authorized.
Eval is parked except regression checks. No protocol, field, challenge,
catalog, transcript, verifier or security-parameter changes in this run.
Exact arithmetic and current security guarantees remain unchanged.

Accepted Jolt554f6270309a734d5bc8541d89d7f0b78f42fd00;
Akita d756e3a67954fb39efa03809ec59971b15303860. Source worktree:
/private/tmp/akita-kernel-campaign-20260906, clean at launch.
Production onehot.metal SHA256:
065827662f06ed94de4974f336349abbb933f72316b0590e1e68c3f6db188c83.
Full-proof parent ../akita-five-workload-matrix-2026-09-05/resume/bin/modular_benchmark,
SHA25672417a5f0fdeed24df3ffc52eb531761ee6df25f0fa692bedac92da056bce90c.
Preserve all preexisting user edits in main and other worktrees.

## Evaluator and gates

Freeze release modular_benchmark, features prover-fixtures,metal,profiling,
--name NAME --scale28 --backend metal --format none. Exactly one
PROOF_VERIFIED backend=metal value=true, exit0, correct workload/trace shape.
Instrumented and synthetic component measurements cannot promote production.
Builds, CPU validation and GPU observations are serialized under the shared
../akita-10mhz-studies/scratch/machine.lock when they could contend.
120s cooling before separate large component processes and full-proof runs;
bounded intra-process ladders use one documented warmup and matched ordering.
5s per standalone GPU command,180s process cap,88GiB process-family RSS stop,
90GiB absolute ceiling, zero swaps and watchdog/abort tolerance. A device
watchdog is a real bug: stop the mechanism, preserve evidence, investigate.

Epoch1: <=4 diagnostic/candidate transactions, checkpoint by06:55 UTC.
Each mechanism gets <=35min or two failed variants without new causal data.
Routine gate <=5min wall including cooling, excluding logged compilation.
No full proofs until target-component evidence predicts >=1s exposed saving
or one explicitly registered production-input capture is required to resolve
a proxy mismatch. No broad sweeps or CPU-reference reruns.

Finalist validation is a separate reserved <=40min/12-proof epoch:
Fibonacci ABBA, then one parent/candidate pair for BTreeMap/SHA2/SHA3/Collatz.
Accept >=1s Fibonacci mean saving, >=3% higher arithmetic-mean MHz across5,
and no observed per-workload regression. Any negative transfer is inconclusive
or rejected, not silently averaged away. One predeclared targeted refresh
may resolve an ambiguous cell; never rerun until lucky. Freeze the exact
finalist and manifest before launching. Reassess >3% same-session parent
drift or a fresh-parent discrepancy >5% against the current same-format
reference; do not reuse the obsolete b160 absolute drift window.

Before integration: independent touched-family parity including selected
zero/tails, serial nextest Metal suites and affected PCS serial/parallel,
both Jolt clippy modes, applicable fork checks and fmt. Report inherited
failures explicitly. No optimality claim without validated machine limits.

Editable: isolated fork commit kernel/runtime, narrow diagnostic host tools
here, isolated Jolt integration only if justified. Evaluator, accepted source,
raw logs and past events are frozen. Analysis and contract are controller
owned; candidate code cannot revise its own bar. Commit preregistrations
before corresponding kernel changes. State: analysis.md, events.jsonl,runs/.

09:59UTC parent-drift amendment: initial finalist epoch stopped after one
verified parent and no candidate proofs, on a9.25% faster historical baseline.
Allow exactly one additional parent stability control before any candidate;
combined budget13proofs/45minutes of measurement and cooling, otherwise the
same twelve-proof paired sequence and unchanged acceptance thresholds. The
additional parent must agree with32.835085250s within3%, else timing remains
paused. Details and immutable recovery artifacts:finalist-rebased/CONTRACT.md.
No more refreshes or candidate timing retries are authorized by this amendment.

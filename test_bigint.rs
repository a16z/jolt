use ark_ff::BigInteger256;

fn main() {
    let a = BigInteger256::new([1, 2, 3, 4]);
    let b = BigInteger256::new([5, 6, 7, 8]);
    
    // Try to multiply
    // What methods are available?
    println!("Testing BigInteger256 multiplication");
}

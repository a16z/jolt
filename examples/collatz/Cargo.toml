[package]
name = "collatz"
version = "0.1.0"
edition = "2021"

[dependencies]
jolt-sdk = { path = "../../jolt-sdk", features = ["host"] }
guest = { package = "collatz-guest", path = "./guest" }

[features]
icicle = ["jolt-sdk/icicle"]
[package]
name = "z3-verifier"
edition = "2021"

[dependencies]
paste.workspace = true

jolt-prover-legacy = { path = "../crates/jolt-prover-legacy" }
common = { path = "../common" }
tracer = { path = "../tracer" }
z3 = "0.20.0"

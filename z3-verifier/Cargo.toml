[package]
name = "z3-verifier"
edition = "2021"

[dependencies]
paste.workspace = true

jolt-prover-legacy = { path = "../crates/jolt-prover-legacy" }
jolt-common = { workspace = true, features = ["std"] }
tracer = { path = "../tracer" }
z3 = "0.20.0"

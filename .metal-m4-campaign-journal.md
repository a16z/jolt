# Jolt Apple Metal Backend Campaign Journal

## CAMPAIGN CLOSE-OUT (10:30 ET Jul 30) — ALU LIMIT REACHED
**Trunk gpu/metal-backend @ef53a2c67 (NOT pushed). Full gates: byte_diff 19/19 BOTH modes, 262/262 metal crates, clippy ×3.**

### Final ladder (quiet box, checksummed ee728d71, ABBA medians)
| scale | optimized (same tree) | metal | same-tree | vs campaign-start (41.66/8.56/1.36 @bf802c135) |
|---|---|---|---|---|
| 2^16 | 1.30 | 1.17 | 1.11× | 1.16× |
| 2^20 | 7.52 | 3.54 | **2.12×** | **2.42×** |
| 2^23 | 37.74 | **16.75** | **2.25×** | **2.49×** |
| 2^24 (single-run) | 74.27 | 47.42 | 1.57× (swap-bound; carve seam = fix) | — |
- **16.75s sits INSIDE the measured ALU-limit band (16.5-17.5s)** — the perfect-overlap projection from measured device (1.65 Gmul/s = 84% of mulhi roof) + CPU rates. No remaining >0.5s structural lever (W11 audit + R3 blind-spot sweep concur).
- CPU tier itself got **1.10× faster** as campaign fallout (41.66→37.74: W8 walk fusion, W10a lane reuse, W10b fresh multi-Miller — all [CPU-side] commits).
- Memory: footprint ledger (ps-RSS discredited — compressor artifact), st6b live-set −1.35GiB, 8.30GiB stable @2^23, 2^24 completes on 16GB both backends.

### Waves 6-11 deltas (@2^23 metal arm)
W6 Miller 25.35→23.1 · W7 st8 concurrent (+probes: 84%-of-roof, small-limb NO-GO) · W8 walk fusion →~21.5 · W9 InstructionInput →20.25 (+3 refutations) · W10a lane-reuse+arena →~19.0 · W10b fresh multi-Miller (CPU arm −1.0) · W11 two-phase overlap →16.75 + 2^24 probe.

### The refutation ledger (measurement culture — what died at attribution)
R1: small-limb (hardware NO-GO 0.37-0.52×), az/bz pool (0.25s), 4s-walk framing (rayon-parallel), clock-ramp (pre-eaten), [0,2p) fr-side (depth-1 nets 0) + prod transfer (+0.27s WORSE). R2: 7 of 9 dead as-specced (witgen pool 0.107s, split-eq 0.16s, gnark schedule already-in-arkworks, CS10 loses-to-Scott, concurrent encoder per-CB-win-lane-loss, multi-queue, torus-GT) — but attribution passes found the real neighbors each time (lane reuse, fresh multi-Miller, two-phase overlap). W6 fixture flattery corrected (14.87→19-22µs/pair).

### What remains (named, unbuilt — for a future mandate)
Polish ≤1s total: st8 reduce tail 0.3-0.5 · st1 uniskip residual 0.3 · IRR::prepare decode 0.4. Memory/capability: materialize-into-carve (st6b 2.3GB + fixes 2^24 swap → est ~37-38s there) · NTZ 2025/611 small-space Jolt (transcript-identical, TRACER synergy) for 2^25+. Bigger silicon: M4 Max/Ultra (4× GPU, 2-4× bus) would re-open every ALU-bound pool — the backend carries over unchanged.

### First-of-kind note
Per R2/R3 literature sweeps: first published-class Miller loop on an Apple GPU; 6.63µs/pair leads published GPU records ~3-6× normalized. Publishable if the user cares.


- 08:4x ET Jul 30: **W10a FINAL — witgen pivot + arena landed** (branch gpu/metal-w10a-witgen @c8d497f8a, 5 commits, every unit gated byte_diff 19/19 ×2 + muldiv host/host,zk + kernels 211/211 + workspace clippy host/host,zk/metal). **Checkpoint #1 refuted R2 #1 (discount prior 3-for-3): the modular stage recipes TILE the prove window — between-stage residual 0.000s @2^22; "outside-stages 3.5s" was legacy-harness geometry.** GPU-shapeable Vec<F> materialization (oracle_table, ALL of it) = **0.107s** « 0.8 gate ⇒ pivot. What the fine spans found instead: three REDUNDANT witness re-walks inside prepares re-deriving lanes the W8 record already parked (SharedInstructionRows lives to session end — retention free). **Landed as [CPU-side] lane-reuse:** U1 metal-IRR st5 reclaim (prepare 0.40/0.47→0.081/0.088 — the metal slot predated W8 and ignored the parked rows; optimized twin already reclaimed) · U2 st7 HWCR pushforwards read shared rows (st7 0.265→0.102) · U3 st8 opening columns project shared rows + 2-lane inc walk (MJO::prepare 0.290→0.124). **Wave ABBA @2^22 (checksummed c4fc788f vs e61ce1c7, tight pairs): 10.78/10.79 → 10.13/10.16 = −0.64s (−5.9%), mechanism-matched (Σ windows 0.65). @2^23 proj ~−1.2 (U2/U3 help the optimized arm too — shared paths).** **Arena (R2 #6) re-landed on the seam measurement supports:** NOT MTLHeap (tier is host-backed no-copy by design), NOT st0/st8 (census: st0 = 32×18MB sequential, st8 = zero traced device transients) — the transient mass is st5→st6b, where whole-buffer smallest-fit served ONE adoption family and four allocated 3.5GiB fresh. Retired buffers now adopt as ArenaSlabs (page-aligned carve, leases return on drop, strong-held only until first carve → pages free exactly when un-pooled buffers would); RoundTable bind targets take the uninit path (kills ~1.4GiB single-threaded from_elem zeroing @2^23). **Measured @2^23: st6b traced live-set 7104→5760 MiB; boundary footprint st6b 6.07→5.41 / st7 6.42→5.79 / st8 6.64→6.12 GiB; peak footprint UNCHANGED 8.30 (peak window = st5's own fresh 3.4GiB pair + host mass; residual = 5×256MB+1024MB caller-materialized cur tables → needs a materialize-into-carve seam, documented not built).** **phys_footprint instrumentation (R2 rider) SETTLED THE BIMODAL-RSS MYSTERY: footprint peak stable 8.30/8.31/8.31 across runs while ps-peak-RSS wobbles 8.01→8.34 — compressor timing over a stable bill; RSS under-reports up to 3GiB at the st5 boundary (5.22 RSS vs 8.29 fp). Footprint (proc_pid_rusage V4, offsets test-pinned) = the campaign metric from here; JOLT_METAL_ALLOC_TRACE=1 = paired alloc/free events ≥4MiB in any chrome trace.** Landmines: (1) e2e ABBA under W10b co-run load = junk (B-arm hit 14.66 on a 10.7 baseline; in-trace windows + span-presence carried the U1 claim until the box calmed); (2) Drop impls on DeviceBuffer pin pass-scoped views to scope end — alloc-only census events for Metal-owned kinds, never a Drop; (3) muldiv zk trio SIGABRT once under memory pressure, clean alone + on retry ×2. NOT merged to trunk (orchestrator's call; no conflicts expected — fences held, W10b untouched files).

- 02:25 ET Jul 30: **W7 FINAL — probes + dispatch fix** @afc3ab00f (branch gpu/metal-w7-probes off 4ded4e625, 2 commits). **P1 M4 rates table** (mulrate_microbench, sanity-guarded): mul32 494 G/s · mulhi32 248 (0.50× — Turner's M1 ratios hold EXACTLY on M4, "M4 wide-MAD" hypothesis refuted) · mad32 494 (add free) · **ulong-widening pair 247 G products/s (both halves at the mulhi price — fr.metal's idiom is optimal; explicit mul+mulhi = 3 slots at 165)** · add32 1754 (3.6× — carries ride nearly free, explains CIOS efficiency) · add64 458 · mul16/mad16 = mul32 (NO 16-bit rate bonus; R1 #12 = register-footprint only). **Production CIOS = 205-211 G products/s = 84% of the pair roof → only ~16% headroom for ANY mont-mul reformulation (ceiling 1.93 Gmul/s).** **P2 small-limb NO-GO (decisive, hardware-grounded):** w=13/n=20 u32-column SOS measured 0.84 Gmul/s = 0.52× production on identical chained-squaring data — and that's CSE-flattered (0.84×820 mads > 495 roof ⇒ compiler folded the squaring grid); general-mul cap 495/820 = 0.60 = 0.37×. Family: w=14 → 0.67 ceiling, w=15 → 0.86 pre-flush (headroom 2², flushes unavoidable), w=16 impossible in u32 columns. Root cause: 820 mad-slots vs 128 wide-muls = 256 slots — 3.2× more issue slots; ZPrize's 4.2× was vs carry-emulating no-mulhi WGSL. **Do NOT spend a W9 conversion wave.** **P3 [0,2p): +9.5-10%** on 64-deep chains (canon twin ±0.6% of production validates method) — but bind/bind_eval are depth-1 (saved mul-correction = added add-correction, net 0) → NOT wired; recommendation-only: the deep chains are Fq-side (g1/g2/fq12 EC formulas + Miller tower, 11-18 chained muls) ≈ 5-8% of st0/st8 device lanes ≈ 0.3-0.5s @2^23, needs canonicalize-at-store at every host-visible boundary. **P4 dispatch audit** (JOLT_METAL_CB_TRACE per-CB tracing SHIPPED in runtime.rs — GPUStartTime/GPUEndTime window + dispatch mix, free when unset): st0 138% util (pipelined ✓), st5/6b 64-88% (FS-bound floor, unreclaimable), hamming/inc tails negligible (~4ms) — **the one real hole: st8 reduce rounds 33-55% util, every fly/fold CB waited synchronously, CPU idle-blocked.** Fix: vendored reduce_and_fold.rs runs each message/fold phase's independent computations (D1L/D1R, D2, E-MSMs; C±, 4 cross MSMs; G1∥G2 folds + s-folds) on rayon join trees, **gated on the device multi-pair hook being installed** (pure-CPU path schedule-identical; JOLT_DORY_REDUCE_SEQUENTIAL=1 = A/B kill switch). In-trace st8 windows: @2^22 1.30→1.13-1.17 (−11-13%), @2^20 0.77→0.68 (−11-12%); concurrent arm strongly load-robust (sequential inflates to 1.8-3.9s under co-running builds). Gates: byte_diff 11/11 metal device-forced + twins/lib 8/8 + muldiv 3/3 ×2 + 251/251 kernels+dory + workspace clippy host/host,zk/metal. Stretch #9 (function constants) declined: no Xcode counters for the occupancy diagnosis, wrong ROI. e2e ambient junk (load 13-25, W8 co-running) — final ladder needs quiet box; expected @2^23: st8 −0.2-0.3s.

- 23:46 ET: **W6 checkpoint #1 — GO both lanes** @4a4bd9002. Fq6/Fq12 tower landed (arkworks-mirrored Karatsuba/CH-SQR2/mul_by_034, ξ=9+u shift-add, constants generated+fail-closed-asserted). **Register-pressure risk RETIRED: 0.82-0.98 Gmul-eq/s in-tower (~2× bare-Fq cost, no spill crater).** Miller: device table-kernel 6.63µs/pair vs CPU 14.87 = **2.24×**; fly-kernel 10.64 vs 22.72 = 2.14×; occupancy wants ≥4k threads (ppt 2-4); coeff table 137MB shared across 40 cols (566MB fear = wrong geometry); 0% co-run degradation (both ALU-bound). Projections: st0 GPU lane ~3.7-3.9 (becomes bound), st8 ~1.1→0.4-0.5, e2e ~23.4-24 vs ≤23 target. ORCH STEER: hybrid CPU/GPU pair split (~30/70, Tier2Accumulator partition-invariance, env-tunable) → st0 ~3.2, worth ~0.7s; final gates must be full 19/19 ×2 (checkpoint listed 11/11 subset).

## RESEARCH LANE (user directive 23:31 ET — standing, one agent per wave cycle)
Survey Apple-GPU/Metal compute + GPU-ZK literature → ranked concrete proposals (idea/stage/est-seconds/cost/risk/link) mapped to measured profile; winners feed wave planning; proposals + verdicts logged here. Idea-level only, no code reuse. **R1 spawned (726ea537, fable-high)**: Turner metal-benchmarks + Asahi AGX docs + WWDC compute guides (register-file/occupancy cliffs — W6-urgent), mopro/Icicle/metal-msm precedents, GPU-pairing register-pressure literature, Emmart-style limb tricks, st1-4/witness-decode offload evidence. Sections: ranked ≤12, W6-urgent, literature dead-ends.

- 08:55 ET Jul 30: **W10b FINAL — pairing wave: R2 #4 premise refuted, structural rewrite landed on the OPTIMIZED arm; R2 #5 (CS10) refuted, not built.** Branch gpu/metal-w10b-pairing @18c1ddf64, 3 commits (attribution bench f400dc832 / fresh loop e79ec9412 / task grain 18c1ddf64). **Attribution (production-shape fixture: 32 windows × 40 cols × 256 DISTINCT preps = 328k pair-evals — W6's 14.87µs/pair was flattered by its 128-recycled-seed fixture; true ark absorb-real = 19.4-22.4µs/pair):** (1) mul-SCHEDULE gap does not exist on M4 — ark-ff's generic aarch64 path already implements gnark's no-carry CIOS and LLVM out-schedules a hand version (fresh CIOS 0.82-0.87×, lazy-Fq2 0.84× — both LOSE); gnark's 2.25× cite is x86-asm-relative. (2) Clone tax ≈ 0. (3) arkworks multi_miller_loop re-chunks pairs by 4 internally → 16 shared squarings/pair. **Landed instead (parity-pinned, byte-identical): fresh prepared-coeff multi-Miller** — one shared ladder per slice + Scott pairwise line combining (fq12.metal's formulas as Rust twins) + borrowed coeffs + batch normalize = 1.2-1.4× absorb-shape all-core; task grain = the knob that made it real in-pipeline (MILLER_CHUNK 512→128, absorb 64-pair sub-shards merged per column — coarse serial tasks cost +0.04 metal st0 until re-grained). **Results: optimized-arm st0 −1.0s BAAB @2^22 (8.15/8.23 v 9.15/9.19) / −0.7-0.8 @2^23 (12.24 v 13.00 loaded; 10.37 v 11.06 quiet) — the same-tree ratio denominator improves ~2%, honest. Metal-arm st0 WASH (absorb hides under the device dispatch window; share sweep post-change confirms 0.5 < 0.55 < 0.60 — default UNCHANGED, the balance is device-share-coupled: raising s shrinks the dispatch window it must hide under), metal e2e −0.1 @2^22 (3v3 clean both orders) / −0.26 @2^23 (1 pair).** **CS10 n=2 refuted with receipts:** merge coverage 11 zero-zero of 32 windows; with measured op costs the merged fold (18 Fq eval + dense Fq12 mul 1329ns = 1480ns/pair/window) LOSES to Scott-combined 2-step (1347ns) which W6 already banked device-side and the fresh loop banks CPU-side; they don't compose (dense fold kills cross-pair combining; cross-pair merged products not precomputable — row pairs vary); fly kernel ineligible (G2 varies per call); would-be zero-zero-merged table @2^23 = 320MB (v 274 base, fits 16GB — moot). R2's 15-20% assumed the Scott-less baseline. Gates ×2 per commit: dory 45/45 + kernels 211/211 + byte_diff 11/11 metal-forced + muldiv 3/3 host & host,zk + clippy host/host,zk/metal. Landmines: (a) W10a co-run poisons CPU-share sweeps specifically (high-share arms suffer more — sweep ONLY quiet, adjacent-pair); (b) benchmark harness needs repo-root cwd (guest compile) and traces land in benchmark-runs/perfetto_traces/ — glob narrowly, a loose *.json glob ate firebase.json; (c) macOS has no flock(1) — uv-python fcntl wrapper on /tmp/jolt-gpu.lock.
- 06:09-06:13 ET: **W9 FINAL: 1 landed + 3 refutations** (ticket 4 [0,2p) g1: built parity-green, **st0 +0.27s WORSE both orders** — isolated-probe +10% doesn't transfer to gather-heavy kernels, fatter lazy bodies cost occupancy; reverted. Order-inverted BAAB = new standard confound-killer; baseline st0 position-stable 2.72-2.74 across 8 runs). **Merged ff → trunk @7374028b9** (288/288 + muldiv ×2, binary d9f1a021).
- 06:13 ET: **W9 LADDER @2^23: optimized 39.61/39.72/39.88 · metal 19.84/20.25/20.67 → median 20.25 = 1.96× same-tree / 2.06× vs campaign-start. 2× CROSSED.** (Metal −1.77 vs prior ladder: −0.5 InstructionInput + fresher box −0.4 both arms + compounding; same-session ratio is the number.) Reported to user.
- 06:1x ET: **W10a spawned (73464b9c)**: outside-stage 3.5s attribution-first (kill <0.15s items, pivot threshold 0.8s) → GPU oracle materialization (risc0 data_witgen shape) + placement-heap arena (peak=max, phys_footprint instrumented). **W10b spawned (156fc367)**: gnark-schedule CPU absorb rewrite (bit-identical Montgomery schedule, re-sweep hybrid share) + Costello-Stebila n=2 merge (table 137→270MB, verify @2^23 fit). Both carry R2-discount priors + attribution gates; fences set (witness/arena vs tier2/miller).
- 08:50-08:52 ET: **W10b FINAL, cross-merged → trunk @f7335131d** (clean, fences held; 256/256 + muldiv re-verified). Fresh multi-Miller: absorb 19.4-22.4 → 15.8-17.5 µs/pair (1.2-1.4×, upside ~1.5×), **optimized-arm st0 −1.0 @2^22 / −0.7-0.8 @2^23** (denominator honesty), metal-arm st0 WASH (absorb hides under dispatch window; share 0.5 re-swept — sharp equilibrium, unchanged), metal e2e −0.1/−0.26. Task re-grain MILLER_CHUNK 512→128 + 64-pair sub-shards (stragglers eat the win at coarse grain). CS10 refuted-by-arithmetic (never built). Landmines: recycled-seed fixtures flatter CPU (distinct preps + production shapes mandatory), share sweeps = quiet-box only, loose *.json glob ate firebase.json (restored).
- 08:52 ET: **W10 CYCLE LADDER @2^23 (quiet, ce0d99e2)**: optimized 38.04/38.26/38.32 · metal 18.31/18.34/18.56 → **median 18.34 = 2.09× same-tree / 2.27× vs campaign-start**; @2^20 7.44/3.64 = 2.04×. Reported to user. Merged worktrees pruned (98Gi free).
- 09:18 ET: **W11 gate: 0.552s @2^22 → GO w/ mandatory pivot.** B1-as-specced DEAD (host between-dispatch ≈ 0.001s/round — 1-fused-CB discipline already amortized sp1-zorch's whole premise; flat-segment prize = 0.044s sync collapse). Real serialization = prove_batch member loop: st6b BytecodeReadRafCycle (CPU, 0.392) runs while GPU idle THEN device members while CPU blocked (−0.439 claimable); st5 RegistersValEval −0.113. **Pivot: two-phase round advance** (optional ProveRounds begin_round/collect_round; device slots encode+commit w/o wait, CPU members run under flying CBs; RLC batched-poly absorb = order-free field sum, prover.rs:230 confirms single absorb). R3 checks: (1) confirmed, (2)(3) moot. @2^23 claim ~1.0-1.1s. Risks named: detached-CB self-borrow (explicit-contract pass type), bus contention (2× headroom).
### R3 verdicts (09:08 ET — notes/metal-campaign-research-r3-2026-07-30.md)
- **Q1: NOTHING MATERIAL REMAINS** — tile-mem/mesh (render-only), sparse MTLBuffer (doesn't exist), compressed textures (private+high-entropy), Metal-4 queues (encoding 2.7µs already), FS batching (spec-fixed, 29ms floor) all dead. Remaining road = named queue → ~16.5-17.5s ≈ the measured 2.4× ALU limit. No hidden second act.
- **Q2 B1 mechanics** (forwarded to W11): shared-challenge + no per-instance absorbs; dense-stacked segments + metadata NOT pad-to-max (27.5GB bloat precedent); pad-deadness analytic (phantom-range gotcha); sp1-zorch's 3.1× was host-dispatch scaffolding collapse.
- **Q3 >RAM**: NTZ eprint 2025/611 = small-space JOLT specifically, transcript-identical, <2× time (likely §C2's formal source; TRACER-215MHz synergy makes re-execution ~100× cheaper than when §C2 was priced) · Blendy 2024/524 O(N^1/k) sumcheck · mmap-backed MTLBuffers work (llama.cpp precedent) for capacity-not-speed. Compressor layouts exhausted.
- 08:5x ET: **W11 spawned (6b10f76e)**: B1 batched rounds HARD-GATED (CB_TRACE decomposition: per-instance-serialization + dispatch-gap share ≥0.5s @2^22 or DECLINE) + bounded 2^24 capability probe (footprint ledger, swap-watched, evidence-only on failure). **R3 spawned (565a116d, 45min cap)**: blind-spot sweep + B1 mechanics cross-check + >RAM proving techniques; "nothing material remains" is an acceptable answer.
- 07:56-08:00 ET: **W10a FINAL, merged ff → trunk @c8d497f8a** (211/211 + muldiv re-verified). 5 commits: U1/U2/U3 lane-reuse (IRR 0.40→0.08, HWCR 0.265→0.102, MJO::prepare 0.290→0.124) + arena + footprint ledger. **Wave: 10.78→10.13 @2^22 (−0.64, mechanism-matched); @2^23 proj ~19.0 ≈ 2.06× same-tree.** Levers named: materialize-into-carve (st6b cur tables 2.3GB), st1 uniskip residual 0.30, st5 peak window (2^24 headroom). Landmines: DeviceBuffer-Drop borrow-pinning, muldiv-zk SIGABRT under memory pressure (watch).
- 07:32 ET: **W10a 3 commits landed**: U1 metal-IRR reclaims record rows (0.40/0.47→0.081/0.088 @2^22) · phys_footprint instrumentation (proc_pid_rusage V4) — **BIMODAL-RSS SOLVED: compressor-timing artifact over a stable bill; footprint peak 8.30GiB rock-steady @2^23 while ps-RSS wobbles/under-reports up to 3GiB (st5: 5.22 RSS vs 8.29 fp). Footprint = campaign metric from here.** · Arena re-aimed by census: NOT MTLHeap (breaks zero-copy), NOT st0/st8 (zero device transients — R2 #6 geometry wrong too); transient mass = st5→st6b: ArenaSlab adopt/carve/lease + uninit RoundTable binds (kills 1.4GiB zero-fill) → **st6b live-set 7104→5760MiB, boundaries −0.5-0.66GiB; peak unchanged 8.30 (st5's own fresh pair — materialize-into-carve seam documented, not built)**. JOLT_METAL_ALLOC_TRACE shipped. U2 next.
- 06:48 ET: **W10b checkpoint #1** @f400dc832 — attribution corrects W6's fixture-flattered 14.87µs/pair → real absorb 19.4-22.4 (128-recycled-seed = cache-resident preps; clone tax truly ≈0). **R2 #4 gnark-schedule DEAD on M4** (ark-ff aarch64 already ships gnark's no-carry CIOS, same hackmd cite in source; fresh hand-schedules measure 0.82-0.87× of ark). **GO variant: structural fresh multi-Miller** (ONE shared squaring ladder per absorb slice vs arkworks' internal per-4 chunking + Scott pairwise combining as Rust twins of fq12.metal): 1.41× measured, ~1.5× upside; st0 proj −0.2-0.4 @2^22. **R2 #5 CS10 REFUTED by arithmetic**: merged fold 1480ns loses to Scott-combined 1347ns and they DON'T compose (dense fold kills cross-pair combining); coverage 22/64 dbl steps; only beats a Scott-less baseline. R2 scorecard now: #1 #2 #4 #5 #7 refuted/dead as-spec'd, #4-adjacent structural win real, #6 arena pending, #3 B1 pending w/ heavy discount.
- 06:35 ET: **W10a checkpoint #1 — PIVOT (gate fired correctly)**: between-stage residual = 0.000s in the modular tree — the "outside-stages 3.5s" was legacy-harness geometry; oracle_table Vec<F> materialization = **0.107s total** ⇒ R2 #1 witgen REFUTED (3rd inflated estimate; risc0 data_witgen shape doesn't transfer — W8 already ate the pool via direct lane consumption). REAL finds: st5 MetalIRR::prepare re-collects what W8 parked (0.285 — metal slot predates W8, optimized twin already reclaims!), st7 HWCR walk ⊂ SharedInstructionRows (0.167), st8 OpeningColumns 3/5 lanes reusable (~0.15). Re-scope: U1-U3 lane-reuse [CPU-side] fixes (~1s @2^23) + arena/phys_footprint. st0 stream (2.41) explicitly left alone (W9 starvation lesson applied).
- 05:52 ET: **W9 tickets 2+3 REFUTED by measurement** (R2 estimate quality flag — two inflated pools): ticket 2 split-eq: eq-build pool is **0.16s @2^22 total** (not ~2s; st5/6b prepare mass = rounds 2.86 + IRR::prepare 0.41) — refutation msg 1642 eaten by the 529 storm, re-flagged. Ticket 3 concurrent encoder: built+gated clean, **ABBA: st0 serial 2.73 vs concurrent 2.99 (+9.6% LOSS both pairs)** — CB_TRACE shows concurrent CBs themselves −11% (R2's per-CB claim TRUE) but the greedy CB starves the pipelined Miller-table CBs it overlaps → per-CB win, lane loss; W7's 138%-util reading was the truth. Reverted; **lesson: audit the LANE, not the CB, before adopting intra-encoder concurrency on a pipelined lane.** Branch = ticket-1 commits only, clean. Ticket 4 ([0,2p) g1) timeboxed next. ORCH NOTE for W10 planning: R2's #1 witgen (1.5-2.5s) and #4/#5 pairing ests now carry a discount prior — attribution checkpoint gates are MANDATORY.
- 04:58 ET: **W9 checkpoint #1 — InstructionInput slot LANDED** @7374028b9 (+seam refactor fdc1c27d1 [CPU-side]): round-0 host exact-integer, device from first bind (fused promote-in-register from W8 lanes incl. i128, 8 tables ping-pong + q-grid), tails+failure CPU. **st3 window 0.4465→0.1795 @2^22 (−60%), e2e −0.28; @2^23 proj −0.5.** Landmine verified pre-existing: zk-mode byte_diff fails at harness setup on clean base (byte_diff.rs:534 "transparent proof must carry clear claims") — zk coverage remains muldiv host,zk. Re-scope received ×5 (delivery-dup symptom again). Ticket 2 (split-eq) started.
- 05:41 ET: 529 storm #2 killed W9 mid-ticket-2 AND the orchestrator; orchestrator task SURVIVED resume this time (context-death fixes deployed 04:50 work); no auto-resume twin; W9 resumed same task_id (WIP in commitment/runtime.rs to assess).
- 04:50 ET: Daemon deploy restart (context-death fixes) stopped W9 — resumed same task_id w/ re-scope recap embedded (split-eq #2 → encoder #7 → [0,2p) g1 → BRC dropped). R2 was already complete.

### R2 memo verdicts (04:34 ET — notes/metal-campaign-research-r2-2026-07-30.md)
- **W9 re-scoped**: +split-eq single-kernel builds (#2, 1.0-1.6s, S-M — outranks original tickets) + concurrent-dispatch encoder (#7, 0.1-0.3s; multi-queue measured DEAD 2×-cap) after InstructionInput; [0,2p) g1 if time; BRC dropped.
- **W10 planned**: witgen-on-GPU wave (#1, 1.5-2.5s — F5 OBSOLETE post-W8: record→columns = risc0's shipped Metal data_witgen shape, thread-per-row + device atomics; walk ~1s = host floor; checkpoint gate = 3.5s attribution first) + pairing wave (#4 gnark-crypto Fp schedule CPU absorb 2.25×-measured → rebalance hybrid, 0.4-0.6s; #5 Costello-Stebila n=2 iteration merging, table 137→270MB, 0.3-0.5s, parity-safe associativity).
- **W11 gated**: B1 batched rounds (#3, ~1.0s honest) — R2's key insight: FS floor was NEVER the cost (305×94µs=29ms); the 64-88% util gap = per-instance dispatch gaps + narrow tails; sp1-zorch 3.1× precedent byte-matched. Gate on CB_TRACE idle-split evidence.
- **#6 arena** (placement-heap st0/st8, peak=max-not-sum, kills bimodal RSS; NOT residency sets — MLX OOM precedent) + phys_footprint instrumentation: fold into W10.
- Checks done: Low Power Mode OFF (no free speed); macOS 26.5.2 → Metal 4 available (cleaner #7 only).
- **New dead ends**: torus/compressed-GT (measured 20-45% SLOWER), cross-ladder RLC pairing (parity break), multi-queue, device-side FS, one-shot eq, Icicle-import. **Our 6.63µs/pair = ~3-6× ahead of published GPU records (GAPS 3080, Hu TECS'23) — first-of-kind stands, publishable.**
- Midpoint stack-up if W9-W11 land: **~16.5-17.5s ≈ 2.3-2.4× = at the measured perfect-overlap ALU limit** — plausibly the last material levers within byte parity.
- 03:51 ET Jul 30: **W8 FINAL, cross-merged → trunk @3cd4abddf** (285/285 metal crates + full byte_diff 19/19 ×2 re-verified). 5 [CPU-side] commits: TraceRecordRow (1 decode/query/PC-map per row vs ~19) → SoA session-parked record → 9 consumers + co-produced RamAccessColumns/SharedInstructionRows/PcRow; st1-4 = ONE walk (was 11). @2^22: metal 11.91→11.00 (−0.91); st6a −0.28 · st3 −0.22 · st2/st4/st5 −0.13 ea. U4 az/bz declined w/ receipts (pool 0.25s — R1 #2 inflated); U5 InstructionInput = best remaining st1-4 lever. Landmines: stale-binary ×2 (checksum + span-presence discipline now MANDATORY), thermal drift 20→24s/2h, peak-RSS bimodal page-cache-dominated (boundary RSS = the metric), NextIsNoop vs ShouldJump strict-variant parity trap.
- 04:04 ET: **W7+W8 CYCLE LADDER @2^23 (quiet, ABBA, checksummed c5df7d6a)**: optimized 39.35/40.14/40.55 (W8 helped CPU too: was 41.66) · **metal 21.89/22.02/23.09 → median 22.02 = 1.82× same-tree / 1.89× vs campaign-start**; @2^20 8.21/4.11 = **2.00×**. Metal peak-RSS crept (9.4-9.75 — the bimodal transient, boundary metrics honest). Reported to user.
- 04:0x ET: **W9 spawned (e50774e9)**: InstructionInput slot (0.4s, 128-bit lanes) + [0,2p) deep-chain shaders g1-first (0.3-0.5s, canonicalize-at-store) + BRC 4th-kernel stretch. **R2 spawned (36db7843)**: B1 batched rounds vs measured FS floor, prepare-side eq/condensation offload, witness-gen endgame (post-W8 shape), newer GPU-pairing results, M4 multi-queue concurrency + residency APIs. Refuted-list embedded so R2 doesn't re-propose.
- 03:08 ET Jul 30: **W8 checkpoint #2 — U0-U2 LANDED** (@e037680d2): st1-4 = ONE trace walk (was 11); TraceRecord SoA co-produces RamAccessColumns, released at st4. @2^22 ABBA: optimized e2e −0.5s (st1-4 3.33→2.92, −12%), metal e2e 11.98→11.60 (−3.2%); boundary RSS −150/−320/−320MB. Per-unit attribution: U0 net-0 (record build 0.61 ≈ outer walk 0.53 saved), pool was U1/U2. @2^23 proj −0.8-1.2s. Reprioritized: U3-middle (st5 rows + regval RdInc + st6a PC, pool 0.58s; record release moves to st6a, NEVER overlaps st6b peak; st7 skipped deliberately) > U4 (pool measured tiny 0.25s — R1 #2 inflated). NOTED: peak RSS BIMODAL both arms pre-existing (5.6-5.8 vs 7.1-7.5 GiB modes, st0/st8 transient — not W8's, watch in ladders). Ops: STALE-BINARY landmine ×2 (fix: span-presence/walk-count checks before trusting arms).
- 02:18 ET Jul 30: **W7 FINAL, merged ff → trunk @afc3ab00f** (251/251 + muldiv 5/5 re-verified). Shipped: mulrate_microbench probes + JOLT_METAL_CB_TRACE per-CB audit + **st8 concurrent reduce-round lanes in vendored reduce_and_fold.rs (rayon join trees, device-hook-gated, JOLT_DORY_REDUCE_SEQUENTIAL kill switch): st8 window −11-13% @2^22, strongly load-robust** (P4 audit: st8 was the one dispatch hole at 33-55% GPU util; st0 pipelined 138%, st5/6b FS-floored — R1 #5's 0.5-1s mostly pre-eaten by W1-W6 discipline). Function constants declined (no counters, wrong ROI). Landmines: vendored crate lacks workspace lints (#[expect] unfulfilled-warns), mul_pow_2 >255 panics, MSL compiler closed-form-folds pure-add chains (circular chains + iters-doubling guard). QUEUED for W9: [0,2p) Fq-side unit (0.3-0.5s).
- 01:36 ET Jul 30: **W8 checkpoint #1 — GO** — 11 walks located + attributed @2^22 (walks 0.94 + extraction-prepare 0.65 + rounds 0.96; adjacent st5/6a/7 +0.72; root cause: extractors memoize NOTHING — 6 lookup_query + 14 decode per row in st1 alone). **R1's ~4s HONESTLY REFUTED → 2.2-3.2s defensible** (walks are rayon-parallel; the @2^23 6.4s wall = walks+rounds+MEMORY PRESSURE — st1 retains outer-rows 1.9GB + az/bz 1GB at 2^23). TraceRecord design: challenge-free SoA lanes, 175B/cycle (1.47GB @2^23) STRICTLY BELOW the ~230B rows it replaces at peak → st1 peak −0.5GB; ram lanes double as RamAccessColumns; decode ONCE. U0-U5 consumer-by-consumer, each byte_diff-gated. Projection: st1-4 6.4→3.5-4.2, **e2e 23.1 → 20.5-21.5 ≈ 1.94-2.03×**. Riders: W7-P4 file-fence flag-don't-race; RSS claim needs measured number; U4 az/bz may be dropped if Spartan resists.
- 01:31 ET Jul 30: **W7 checkpoint #1** @f8ea7e867 — M4 int-ALU rates pinned (mul32 494 G/s, mulhi 248 = 0.5×, mad free-add, widening-pair idiom optimal at 247 products/s, add32 3.6×, NO 16-bit bonus; Turner M1 ratios hold exactly on M4). **Production CIOS = 84% of hardware mul-issue roof → ≤16% headroom for ANY Montgomery reformulation (ceiling 1.93 Gmul/s).** **P2 small-limb NO-GO, hardware-grounded**: w=13 measures 0.52× production (CSE-flattered; true ≤0.37×) — 820 mad slots vs 256 wide-product slots = 3.2× issue cost; ZPrize 4.2× was vs carry-emulating WGSL. R1 #3 refuted, W9-conversion wave killed. **P3 [0,2p): +9.5-10% deep chains, 0 at bind depth-1 → NOT wired (right call); future unit = Fq side (EC formulas + Miller tower, 11-18-deep chains) ≈ 0.3-0.5s @2^23.** Kernel-speed lever (W6's hope of ~2×) is DEAD — remaining levers are structural. P4 (dispatch batching) next.
- 01:03 ET Jul 30: **W6 FINAL — GPU MILLER LANDED, merged ff → trunk @4ded4e625** (251/251 + 19/19 re-verified on merged tree). **@2^23 ABBA: 25.35 → 23.1s median (best 23.02) = 1.80× vs 41.66 baseline; st0 5.41→4.40, st8 3.68→3.22, RSS −0.4GiB.** First Miller loop on an Apple GPU. 8 commits: tower+2 kernels / microbench / hybrid absorb / share 0.5 / vendored pairing_hook (1 module + 3 consults) / fly gate ≥2048 / Scott combining (−2.7% vs −11% theory, register pressure — kept, honest gap) / cross-superchunk batching (recovered 2^23 occupancy, −0.5→−1.0s). Floors: st0 4.4 = decode/normalize + balanced lanes + 60-90ms final exps; st8 residual pairing ~0.55; next lever = kernel speed (mont-mul @50% of mulhi roof). NOTE: no zk-mode byte_diff exists upstream (mixed-mode build broken on untouched trunk — verified, not W6's). Landmines: MTLBuffer thread-confinement (tier-2 lane makes own wraps), drain_miller_lane or pairs vanish, G2Prepared clones = memcpy noise.
- 01:07 ET: Wave reported to user. **W7 spawned (4f4fd139)** on gpu/metal-w7-probes: mulhi-rate table, small-limb w=13-16 probe (GO/NO-GO for W9 conversion), [0,2p) if ≥5%, clock-ramp dispatch batching, function-constants stretch. **W8 spawned (f620830a)** on gpu/metal-w8-tracefusion: st1-4's 11 redundant walks → fused L2-chunked record [CPU-side] + GPU-resident + branch-free az/bz on device; consumer-by-consumer landing, schema+attribution checkpoint gate; fences vs W7. Projection if both land: ~18-19s ≈ 2.2×.
- 00:20 ET Jul 30: **W6 checkpoint #2 — st0 device Miller LANDED** @e56759970: jk_miller_table ragged segments + row-index indirection into per-pass step-major 137MB shared coeff table; hybrid absorb splits per column, device-first + CPU-share overlapped, merge_miller partial-f folds (partition-invariant); first-error latches lane off. **Sweep: CPU share 0.5 optimal (GPU carries tier-1 too — balance ABOVE my 0.3 estimate): st0 4.78→2.82 @2^22 (−41%), e2e 14.29→12.20 (−14%), RSS 5.12→4.74.** st0 now BALANCED (no idle lane; remaining 2.8 = decode/normalize + shares + final exps). 19/19 clarified = 11 byte_diff + 3 engine_twins + 5 lib, all green + muldiv ×2. Next: st8 fly-kernel rounds ≥4k pairs, then Scott combining. @2^23 projection 23.0-23.2 — ≤23 in reach.

### R1 memo verdicts (23:48 ET — full cite memo: notes/metal-campaign-research-r1-2026-07-29.md)
- **Adopted → W6 now**: soft register cliff (Dynamic Caching — optimize LIVE state not occupancy; tg-mem = L1 pool, spill-to-tg worthless, simd_shuffle 2×), Scott pairwise line-combining, 13-M2 sparse verify, function-constants DCE, [0,2p) if convenient; Longa 2022/367 only-if-spilling. W6 architecture VALIDATED (thread-per-pairing + precomputed lines = consensus; first-of-kind on Apple GPU).
- **Wave-planned**: W7 probes+cheap (M4 mulhi microbench #7 + small-limb w=13-16 mont-mul probe #3 [ZPrize'23 M3 4.2× discounted→honest 1.2-1.5×] + [0,2p) #6 + clock-ramp dwell fix #5 [fewer/larger dispatches, never drain queue] + Xcode counters #9). W8 = **st1-4 trace-walk fusion → GPU-resident record (~4s, THE biggest lever; risc0 ships witgen-on-Metal precedent)** + branch-free az/bz expansion. Verify-first: #10 small-value sumcheck (eprint 2025/1117) vs current CPU tier; #11 int+fp dual-issue co-scheduling; #12 16-bit index regs.
- **Literature dead-ends (don't spend waves)**: 9×30-limb u64-acc, float-mantissa limbs, RNS, carry-save/RSD, Barrett, base-field Karatsuba, simdgroup_matrix (no int), GPU sorts, full-GPU MSM <2^17 (mopro/ICICLE data — hybrid+residency stands), GPU final-exp at our batch, cooperative-Fp12-tg. Calibration: 1.65 G/s ≈ 50% of mulhi roof (~2× headroom max, not 100×); Chimera 2026/1388 validates residency+late-round-CPU, e2e 1.27-1.64× bounds.

## W6 MILLER LANE (user directive 22:57 ET — campaign CONTINUES past the 1.62× milestone)
Mandate: GPU on the ~5s pairing mass (st0 tier-2 Miller 43 thread-s @2^22 + st8 wall ~1-1.3s @2^23) targeting ≤23s (1.8×), stretch ≤21s. Vendored dory fair game (user is dory maintainer); branch stays LOCAL (no push). **W6 spawned (fb120ce8)** on gpu/metal-w6-miller @51788b620: Phase 1 = Fq6/Fq12 tower MSL (ξ=9+u, arkworks-sourced constants) + parity + REGISTER-PRESSURE microbench with explicit go/no-go rule (≥1.5× CPU-normalized GO / 1-1.5× overlap-value-only / <1 NO-GO with tower as deliverable); Phase 2 = batch Miller exploiting shared-squaring f-ladder, minimal live Fp12 per thread, final-exp STAYS CPU (42 column exps), Tier2Accumulator absorb hook (st0) + vendored multi_pair (st8), GPU-contention accounting vs tier-1/i128 dispatches. Orchestrator's honest projection: st0 5.6→~4.3 + st8 3.8→~3.0 ⇒ ~23.5s ≈ 1.77×; the 2.1-2.3× estimate needs Miller-on-GPU ≥2× — Phase 1 decides.

## MILESTONE (22:50 ET Jul 29) — 1.62× SHIPPED (campaign continues, see W6 above)
**Branch gpu/metal-backend @51788b620 (NOT pushed; user call — contains vendor/dory-pcs [replace], Apache/MIT-clear, all-original MSL).** byte_diff 19/19 BOTH modes on final trunk; workspace clippy host clean; 243/243 kernels+dory tests.

### Final ladder (sha2-chain, quiet box 22:26-22:33 ET, interleaved ABBA, medians of 3)
| scale | optimized (same tree) | metal | speedup | RSS O→M |
|---|---|---|---|---|
| 2^16 | 1.36s | 1.32s | 1.03× | 396→554 MiB (+ctx) |
| 2^20 | 8.56s | 5.51s | **1.55×** | 2.13→1.74 GiB |
| 2^23 | 41.66s | 25.71s | **1.62×** | 10.49→7.84 GiB (**−26%**) |
Stage walls @2^23 traced: st0 13.09→5.61 (−57%) · st1-4 6.52→6.35 (untouched) · st5 4.72→3.80 · st6a 1.20→0.98 · st6b 7.47→4.27 (−43%) · st7 0.67→0.93 · st8 7.41→3.83 (−48%).
**Baseline forensics:** bf802c135 re-measured same night = 41.75s/10.4GiB @2^23 ≡ trunk-optimized → [CPU-side] seams FREE, and Phase-0's 37.82s/7.92GiB was ambient (box drifts ~+10% after 8h load; identical code). Same-night ABBA = the defensible comparison. vs Phase-0 morning baseline metal is 1.47× cross-condition.

### Verdict vs the 5× ask
**Delivered 1.62× @2^23 (1.55× @2^20), plus −26% peak RSS.** 5× was never physical on M4-base — measured chain: prover is ALU-bound (7% of DRAM roof) → GPU ceiling = 2.8× ALU / 2× streams on-die → Amdahl floors that materialized: st0 tier-2 Miller lane ~4s (CPU-locked), st8 pairing wall ~1s, st1-4 latency streams 6.4s (not GPU-shaped at this geometry), outside-stage witness/setup. Perfect-overlap ALU limit was ~2.4×; we landed 1.62× with the gap = named floors + st5/6b prepare halves.

### What shipped (40 commits, 5 waves + 2 leftover lanes)
25 MSL kernels (fr/g1/g2/kernels.metal), 9 device slots + commit/opening/dory-fold lanes, runtime-MSL objc2-metal stack (no Xcode), zero-copy unified-memory discipline, GLV-4 ψ G2 ladder, signed-digit i128 MSM, Tier2Accumulator overlap pipeline, RoutineHooks + combine_hints guard-scoped seams, vendored dory-pcs (ONE behavioral file: batch-normalize), retired-buffer pool, DEFAULT_MIN_TERMS=2^16 measured. Every merge byte_diff-gated; ~10 honest refutations logged (G2-prep 42% receipt, rayon::join, BytecodeReadRafCycle ROI...).

### Remaining levers (ranked, for a future wave)
1. GPU Miller loops — the ~5s pairing mass (st0 tier-2 + st8 C±/D1/D2), now hookable in vendored dory-pcs. Big build (Fp12 tower in MSL), biggest single prize → would push toward ~2.1-2.3×.
2. B1 cross-relation batched rounds (fractalyze) — st1-4's 6.4s of serialized small rounds + walks.
3. st5/st6b prepare+decode halves (~2s, trace-decode-shaped), per-thread GLV decomposition (~50ms), BytecodeReadRafCycle 4th kernel (~0.2s).
4. 2^24/2^25 capability probe — metal is RSS-LIGHTER; may prove where CPU swaps on 16GB.

### Ops ledger
5 spurious "stopped by user" events (daemon bug, papercut), 2 real 529 deaths (W4 + THE ORCHESTRATOR — subtree severance on respawn, worked around via fresh spawn + disk-state reconstruction), 1 double-resume race (twin stopped), metal-w4 worktree deleted under a live worker + Phase-0 target dirs pruned by unknown cleanup (~40GB freed at 20:30, papercut). fable-max workers: 7 lanes, zero quality failures, multiple honest self-refutations — the measurement culture held.


**Goal:** Apple Metal GPU backend for jolt-kernels on THIS Mac mini M4 (16GB unified). User target: 5× CPU-optimized prover. First deliverable: honest ceiling analysis. Then grind to best achievable.
**Orchestrator:** Pika bg task 9462e7cb on the mini. Workers: fable-max local worktree children (cap 5 — 16GB box).
**Journal untracked** (.git/info/exclude has it + .fractalyze-gpu-techniques.md).

## Hard rules
- Branch `gpu/metal-backend` off origin/perf/kernels-optimized @bf802c135 — ORIGINAL WORK ONLY: no June-CUDA source, no June-bundle commits, pushable. MSL shaders written fresh.
- Fractalyze memo (.fractalyze-gpu-techniques.md) = algorithmic ideas only, NO code/expression reuse — pass rule verbatim to workers.
- jolt-prover library untouched; justified additive seams only (CUDA campaign precedent: visibility-only pub(crate) in optimized/mod.rs OK).
- Byte-diff parity gate 19/19 after every merge (proofs byte-identical to legacy).
- ALL benchmarks serialized (shared GPU + shared memory bus + other agents on mini): flock /tmp/gpu.lock for bench runs only — NEVER wrap nextest in it (CUDA footgun: tests take same lock internally → deadlock).
- Memory: 2^23 CPU prove peaks ~8.8GiB of 16GB. Routine benches @2^16/2^20; 2^23 = milestone gate. Watch worker count during benches.
- sleep_until_child_event between dispatches, never sleep-poll.

## Hardware / toolchain facts (Phase 0, 14:2x ET Jul 29)
- M4: 10 CPU cores (4P+6E), 10 GPU cores, 16GB unified, ~120GB/s LPDDR5X shared bus. Metal 4.
- **No offline metal compiler** (CommandLineTools only, no Xcode). DECISION: runtime MSL compilation via newLibraryWithSource — smoke-tested: 28ms compile, mulhi parity OK, hasUnifiedMemory=true, maxThreadsPerTG 1024. No Xcode dep ever; optional MTLBinaryArchive cache later.
- Zero-copy plan: storageModeShared MTLBuffers. newBufferWithBytesNoCopy requires page-aligned ptr + page-multiple length (16KB pages) — big polys from macOS malloc (>32KB → vm_allocate, page-aligned) usually qualify; runtime check + copy-fallback per buffer (fail-closed). Kernel-tier-owned scratch: allocate page-aligned from the start.

## CUDA campaign lessons (from .gpu-campaign-journal.md — the map)
- 5090 vs 24-core: whole-prove PARITY. Engine rounds slice 2.32× GPU but only ~27% of prove. @2^23 pools: prepares ~9s, stage0 commit 5.85s, stage8 opening 3.71s, rounds 3.03s (305 rounds ≈ 10ms/round = launch-bound).
- Slot pattern: CudaX{fallback:OptimizedX} + threshold env gate + host-twin round_poly_from_skipped_evals; byte parity = round polys byte-identical.
- byte_diff gate: force all threshold envs to 0 so 2^16 fixtures exercise device paths.
- Never flock-wrap nextest. Prewarm pipeline states. Small-tail rounds: CPU finishes below threshold width (F5).

## Fractalyze memo priorities mapped to Metal
- A1 two-launch round budget: Metal = one MTLCommandBuffer per round, 2 compute dispatches (round-poly reduction + fused fold w/ claim math in tail). Encoding is cheap; commit+wait per round ~50-100µs × ~305 rounds = acceptable. MTLSharedEvent > waitUntilCompleted where it matters.
- A2 graph replay analog: pre-encoded MTLIndirectCommandBuffer per width-class OR just re-encode (Metal encoding is far cheaper than CUDA launch setup) — measure first (G rule).
- A5 FS gap: challenge written by CPU directly into shared buffer — no upload step at all on unified memory.
- B1 cross-relation batched rounds (even-padded flat segments, RLC as batch axis): THE structural multiplier; gate on launch audit.
- C1 (p0,diff) carry: 1.5× vs 2× live area — extra valuable on 16GB unified.
- E1 gather-not-scatter; E2 schedules built once host-side (free on unified — no upload); F1-F6 negative results inherited; G host-vs-device split per region before optimizing any lane.

## Mini CPU baseline (perf/kernels-optimized @bf802c135, sha2-chain, medians of 3)
| scale | wall | peak RSS |
|---|---|---|
| 2^16 | 1.36s | 255MiB |
| 2^20 | 7.31s | 1.55GiB |
| 2^23 | 37.82s | 7.92GiB |
Mini ≈ 1.5-1.6× slower than CUDA box's 24-core CPU (expected).

### Stage walls @2^23 (traced 39.16s; scale ×0.966 for untraced)
stage0 11.23 (commit) · st1 1.98 · st2 1.05 · st3 1.15 · st4 1.15 · st5 4.54 · st6a 0.82 · st6b 6.89 · st7 0.57 · st8 6.06
Thread-seconds hotspots: stream_process_one_hot_chunks 64.2 ·  multi_pair_g2_setup 46.3 (tier-2 pairing lane, ~4.6s wall — host-locked) · stream_finish_one_hot 45.8 · sumcheck_round 12.1 (305 rounds) · stream_witnesses 11.4 (13 walks — prepare dedupe target) · HomomorphicBatch::prove_batch 5.5 · Dory open 3.9. @2^20: stage0 3.12 of 8.10 traced (38%!), st8 1.89.

## Ceiling analysis (v1, pre-W1-microbench — the honest read)
- SAME 120GB/s bus for CPU+GPU: bandwidth-bound streaming (binds, folds, table walks) = GPU ≤ bus/CPU-effective ≈ 1.5-2× max (CPU bind is compute-limited ~40-50GB/s effective, GPU can approach bus cap).
- ALU-bound field work (mont_mul chains, EC point adds): GPU est 0.8-1.5G mont_mul/s vs CPU ~0.45-0.6G/s → **2-2.5×**. W1 microbench will pin this.
- GPU-favorable pool: commit tier-1 (G1 adds = 11 field muls each, ALU-dense, low BW). Host-locked: tier-2 pairings (~4.6s wall @2^23), witness gen, transcript, glue (~2.5s).
- Unified-memory edge vs CUDA lane: zero upload/download (CUDA's prepare-overhead killer is structurally absent), CPU/GPU co-execution pipelining (overlap stage k GPU rounds w/ k+1 CPU prepare; commit tier-1 GPU ∥ tier-2 CPU pairing).
- Per-pool budget @2^23 (now → Metal target): commit 10.8→5-6 · st6b 6.7→3.5 · st5 4.4→2.5 · st8 5.9→3.5-4 · st1-4+6a+7 7.0→4.5-5 · serial ~2.5→2.5. **Total ~37.8 → 21-23s = 1.6-1.8×; ~2× stretch** with B1 batching + aggressive overlap.
- **5× verdict: not physical on M4-base.** 5× = 7.5s whole-prove; host-locked pairing+witness+serial ≈ 7s alone before any sumcheck/commit work. GPU is ~2-2.5× the CPU in ALU and 1× in bandwidth on this die — the 5090 (15× bandwidth, 24× ALU of M4-GPU) only reached parity vs its 24-core host. Path to bigger numbers = bigger Apple silicon (M4 Max: 4× GPU cores, 400-546GB/s — this backend would carry over and plausibly hit 2.5-3.5× there) or algorithmic changes outside byte-parity scope.

## Roofline MEASUREMENTS (14:45-15:05 ET — user challenged the asserted BW-bound claim; he was right)
- sudo/powermetrics: hard-denied for this bg session → approved fallback used (measured roofs + GPU-probe + analytic model).
- **Measured roofs**: CPU 10-thread triad 83 GB/s, copy 105 GB/s; GPU triad 96 GB/s solo. **CPU+GPU concurrent = fixed ~95 GB/s pie** (CPU 38-44 + GPU 48-55).
- **GPU-probe-during-prove method** (no root needed): continuous 384MB-array GPU triad w/ 10ms duty gaps, per-pass GB/s timestamped, run across a full 2^23 prove. Probe solo 90-96 GB/s. DURING PROVE: probe holds 72-85 GB/s in every stage → prover DRAM draw ≈ 11-24 GB/s ALL STAGES (st0 ~19, st5 ~20, st6b ~17, st8 ~11). Prove wall +3.1% under probe (37.07→38.22s) — prover doesn't feel bus pressure.
- **VERDICT: CPU-optimized prove is ALU/compute-bound (~800-940% CPU, dips at st0 edges/st7), sits at 13-25% of DRAM roof.** NOT bandwidth-bound. ~70 GB/s of bus headroom exists for the GPU to compute WITHOUT hurting the CPU.
- Ceiling implications (v2 draft, pending W1 mont_mul microbench + traffic-model worker corroboration):
  - GPU ALU (est 2-2.5× CPU chip-wide) is ADDITIVE under co-execution — combined system ALU ≈ 3-3.5× CPU alone, bus not a constraint until ~4× current draw.
  - Revised realistic: **1.8-2.2× whole-prove, stretch ~2.5×** (perfect-overlap ALU limit ≈ 37.8/3 + serial ≈ 15-17s). Hard Amdahl cap unchanged: host-locked ~7s (pairings 4.6 + witness gen + glue) → even infinite GPU = 5.4×; 5× stays out of reach but the gap narrowed.
  - Plan change: co-execution/pipelining is now FIRST-CLASS (not a W4 nicety) — every W3+ slot designs for concurrent CPU+GPU, not CPU-idle-while-GPU-runs. Threshold gates stay (F5) but the default posture is overlap.
- Method caveat logged: "96 − probe" is first-order (arbitration not perfectly subtractive); corroboration = analytic traffic model (worker b331b9c3) should land in the same 10-25 GB/s band.

## Traffic-model verdict (worker b331b9c3, 15:23 ET — full table in task update #1431; receipts verified to file:line)
- **Corroborates probe: whole prove averages ~6GB/s = 7% of DRAM roof. NOTHING is bandwidth-bound.**
- **Mul mountains = st0 + st8** (17.3s = 49% of stage wall, 7-8G EC muls at 80-100% of achieved EC capacity ~0.5G/s): st0 = 1.85G tier-1 one-hot G1 batch-adds + 2.2G tier-2 (328,704 pairings, 42% wasted on deliberately-uncached G2 setup re-prep, scheme.rs:114-119) · st8 = 1.25G combine_hints plain double-and-add scalar muls NO GLV (largest single term) + 0.56G pairing + 42 full-T fold_rows passes over 512MiB.
- **Stages 1-7 (17.1s) are NOT FLOP-bound** (<30% of both roofs; true fmadd capacity 1.5-3.6G/s makes their %ALU ~4× lower than naive): st5 = 46 sequential passes at 14GB/s effective (0.75s at roof, takes 4.5 — memory-LATENCY + SLC-crossing inflation) · st6b = ~7GB fresh allocs (page-zeroing 1-2s) + per-round 403MB re-gathers · st1-4 = 11 redundant full-trace re-reads (RamInc streamed 2×), az/bz 2×2^24 Fr materialized.
- Geometry @2^23: log_k_chunk=4 → 40 one-hot polys × 2^27 coeffs, Dory 8192×16384 grid; IRR 16 phases.
- **Plan consequences:** (1) st0/st8 = the real GPU FLOP targets (W3a running, W3b=st8 next: combine_hints MSM-shaped on device + fold_rows device streams). (2) St1-7 GPU value = LATENCY-HIDING, not FLOPs: GPU streams at 88-97GB/s where CPU's sequential scans achieve 6-19 — convert big scans/gathers (st5 phase scans, st6b materialize+gathers, st1-4 trace walks) to device streams ≈ 2-4× those pools' stream time; plus alloc-churn fixes. (3) **Upstream CPU-tier gifts (NOT metal's to take)**: G2 prepared-cache priming (−0.93G st0 + −0.2G st8) and GLV/Pippenger combine_hints (1.25→0.3G) are CPU-side fixes helping optimized() too — route to CPU perf lane; metal measures honestly against whichever baseline exists.
- Ceiling v4 (post-model): st0→4.5-5.5 (GPU tier-1 ∥ CPU pairings) · st8→3-3.5 · st1-7→10-12 (latency-hiding+fusion) · outside-stages 3.7 → **~21-24s = 1.7-2.2×; band unchanged, composition corrected** (less from ALU additivity, more from latency-hiding).

## Wave plan
- **W1 (infra, RUNNING)**: metal feature, runtime-MSL compile, CIOS field lib from MontgomeryConstants, buffers (no-copy wrap + PageAlignedVec), JoltBackend::metal() all-fallback, --backend metal, parity tests, microbench (dispatch latency, bind GB/s, mont_mul G/s, bus-contention co-run). → ceiling v2 from its numbers.
- **W2 (pattern + gate)**: claim-reduction family (inc, hamming_weight, instruction, registers, ram_ra) + ram_raf/ram_hamming_booleanity dense-fused; MetalX{fallback} + JOLT_METAL_* gates; byte_diff metal variants (force thresholds 0 at 2^16). Perf-neutral pool — goal is pattern + parity gate.
- **W3a (commit lane, the money)**: Metal G1 one-hot tier-1 row commits + MSM in MSL, superchunk-pipelined with CPU tier-2 pairing. stage0 11.2→≤6.
- **W3b (big sumchecks)**: st6b family (IRR cycle, InstructionRaVirt) + st5 (booleanity, spartan product): fused bind+eval 2-dispatch rounds (A1), (p0,diff) carry (C1), materialize-diff-once (F2).
- **W4**: st8 HomomorphicBatch device folds + opening prep; st1-4 prepare table builders + stream_witnesses dedupe (E2 one-shot schedules); B1 cross-relation same-width batched rounds; co-execution scheduler.
- **W5**: 2^23 milestone bench, threshold sweeps, contention tuning; 2^24 capability probe if headroom.

## W1 microbench (M4 10c GPU, min over warm passes) — ceiling v3 inputs
- D1: CB sync round-trip **94µs**; 100 dispatches/1 CB = 2.7µs/dispatch → batch all dispatches between FS points into ONE CB.
- D2 bind: 2^20 dev 60GB/s vs CPU 47 (1.28×) · 2^22 88 vs 48 (1.83×) · 2^24 **97 vs 48 (2.03×)** — device saturates bus, CPU bind caps ~48. GPU clock ramp: short bursts ~60, sustained ~97.
- D3 mont_mul: device **1.65 Gmul/s** vs CPU all-core 0.59 = **2.8×** (1T: 0.10).
- D4 both-sides-streaming contention: dev −52-56%, CPU −19-23%, combined ~80GB/s — never co-run two bandwidth-bound phases; fine when CPU side is ALU-bound (probe evidence: dev held 72-85 during real prove).
- JOLT_METAL_MIN_TERMS default **2^18** (bind break-even; denser kernels cut over lower; per-slot overrides exist).
- **Ceiling v3: combined ALU 1.65+0.59=2.24 Gmul/s = 3.8× CPU ideal; whole-prove realistic ~2-2.5×, stretch ~2.8** (host-locked ~7s Amdahl cap → 5× impossible; 5.4× absolute cap even with infinite GPU).
- W1 deviations (sound): stricter no-copy eligibility (page-multiple OR ≥32KiB); DeviceBuffer<'a> borrow-scoped (W2+ adds owning variant). Transient nextest LEAK flag on env test once — watch.

## Status log (waves)
- 15:18 ET: **W1 COMPLETE** @26c208c8b (3 commits; incl. orchestrator mid-stop snapshot 4d7c50a3c — W1 was stopped 15:04 by a Pika infra bug [mis-scoped cleanup from an unrelated task steer, filed], resurrected, finished clean). Gates: fmt/clippy±metal, nextest 151+162, prover 8/8, fibonacci 2^14 metal E2E verifies. **Merged ff → gpu/metal-backend @26c208c8b.**
- 15:2x ET: W2 (aef446d6) spawned on gpu/metal-w2-slots: inc_claim_reduction + hamming_weight + ram_hamming_booleanity (+ram_raf stretch), slot pattern (MetalX{fallback}, 1-CB-per-round, host-twin round_poly), byte_diff metal arms w/ MIN_TERMS=0 + device probe counter. W3a (b19f6949) spawned on gpu/metal-w3a-commit: Dory commit lane — Phase 1 measure/map stage0 split (tier-1 G1 vs i128 feeds vs tier-2 pairing) w/ mandatory checkpoint, then G1-in-MSL (Fq CIOS + Jacobian, original), superchunk pipeline CPU∥GPU. Target 11.2→≤6s.
- 22:02 ET: **W5b FINAL** — 4/4 tickets @72cb8916f: i128 device (st0 −12% @2^22) · RavDriver third consumer (0.269→0.086s; BytecodeReadRafCycle honestly declined ~1.5h/0.2s) · retired-buffer pool (RSS 5.25→5.05) · **DEFAULT_MIN_TERMS 2^18→2^16 from all-slots-live sweep** (2^14 and 2^20 lose; no per-slot overrides needed). **Merged ff → trunk @72cb8916f**, 192/192.
- 22:14 ET: **W5a FINAL** — 10 commits @b4ccb2e3a: vendor byte-exact → tracing spans → jk_g1_scalar_mul_add → **g2.metal (Fq2 Karatsuba + complex squaring, Jacobian twist a=0)** → RoutineHooks seam → device fold lane → gate calibration → batch-normalize pairing prep [CPU-side, THE one behavioral vendored file +76/−67] → **GLV-4 ψ-decomposition G2 ladder (254→~64 iters, ψ=conjugate+1 Fq2 mul, zero inversions)** → GLV gate retune. Folds 0.802→0.382 (−52%); window 2.49(map)→~1.54-1.65. rayon::join REFUTED by measurement (pairings already saturate 10 cores) — not landed. Remaining floor: pairing wall ~1s (levers named: proof-scoped prepared-G2 cache, GPU Miller). Ladder-latency-floor lesson: gates set by ladder latency not throughput (GLV-4 7ms vs plain 21ms lane-wave). byte_diff 19/19 ×2 post-GLV. **Cross-merged → trunk @51788b620 CLEAN (0 conflicts), 243/243 kernels+dory.**
- 22:15 ET: ALL LANES COMPLETE. Endgame gates running (full byte_diff ×2 + workspace clippy + ladder binary build) → quiet-box ladder next (box idle, workers done).
- 21:12 ET: **W5b ticket 1 LANDED** @d7fb34155 — i128 RdInc/RamInc commits on device: jk_g1_seg_sum learned signed gathers (bit-31 = negate base), i128 → signed base-256 digits count-sorted into (col,window,slot,magnitude) buckets, rides the per-superchunk CB as 2nd dispatch vs resident SRS; tier-2 reduces via descending running-sum + Horner to exact msm_i128 value. **stage0 @2^22: 5.51→4.85 (−12%), e2e −0.5s** (ABBA pairs); removed 4.4-4.5 thread-s of driver-lane contention (@2^23 ~10 thread-s → bigger). Gate JOLT_METAL_MIN_TERMS_COMMIT_INC. byte_diff 19/19 device-forced. → ticket 2 (RamRaVirt driver).
- 20:59 ET: **W5a checkpoint #1** — dory-pcs 0.4.0 Apache/MIT dual → vendored byte-exact @4cddc6e1a via [replace] (workspace precedent; patch/replace can't mix), byte_diff 19/19 pre-modification. **DISCOVERY: the "external floor" was mislabeled — ~1.3s of the 2.49s @2^22 reduce window is jolt-dory-side EC via the DoryRoutines trait (OUR code); only 1.19s pairings are vendored-internal.** Fold scalars batch-uniform (one β/α per vector) → row-Horner again; G2 folds 0.586+0.188 v2-init+0.188 MSM, G1 0.29 — device est 1.13→0.15; pairings stay CPU (confirmed by span counts 26/24/26); one vendored diff planned (hook-gated rayon::join MSMs-under-pairings, ~−0.3s). Est window 2.49→1.3-1.5. Approved + riders: CPU-side split for prepared-G2-cache/inversion levers; pin byte-identity under the join explicitly.
- 20:26 ET: **W4b FINAL — st5+st6b lane COMPLETE** @961288df1 (7 lane commits, 2 [CPU-side] seams: CycleTablesDriver + LazyRaDevice). st6b @2^22 ablation: **3.93→1.99s (−49%)** (Booleanity prove_round −57%, RaVirt −69%); st5 −41%. **Quiet-box e2e @2^22: optimized 24.88 → metal 17.69s (−29%), peak RSS 7.26→4.83GiB (−2.4GiB — device-owned uninit tables freed at handoff).** Honest gaps: alloc pooling not attempted; RamRaVirt third driver identified; unsafe Send/Sync + ensure_host contracts documented. 8 exact-dispatch-count parity tests; byte_diff 19/19 re-run per unit.
- 20:27-20:28 ET: **Merged W4 → trunk @a5d82cdc8** (unions: slots/mod.rs + runtime.rs 25-kernel registry + mod.rs installs; `tail -4` FOOTGUN — it hid 2 of 3 conflicted files, always `git status` after merge). Gates: 186/186 metal (1 leaky advisory), muldiv 5/5 both modes.
- 20:28 ET: **USER DIRECTIVE: dory-pcs vendoring UNLOCKED** — the biggest named floor (st8 reduce rounds ~2.5-3s @2^23) now attackable. **W5a spawned (118baadd)**: vendor dory-pcs byte-exact ([patch.crates-io]) → license check → map 13 reduce rounds (G1/G2 MSM folds ~1.6s of 2.66 @2^22; pairings likely stay CPU) → Fq2/G2 MSL if ROI holds → guard-scoped hooks, vendored-unmodified-baseline equivalence test in suite. **W5b spawned (c32fdf10)**: leftovers — i128 MSM→device (st0 −0.5-1s), RamRaVirt third driver, st5→st6b buffer pool, threshold sweep. Fences set (streaming.rs=W5b, scheme.rs hooks+vendor=W5a). Ladder + final report AFTER both land, quiet box.
- 19:40 ET: **W4b checkpoint #2 — st5 COMPLETE**: 975358818 [CPU-side] cycle-round seam (CycleTablesDriver Host/Device, pending-fold parking, ensure_host handoff) + 2cab82bfc device cycle rounds (adopt at address→cycle boundary, ONE flat ping-pong pair fixed-arity, fused jk_irr_cycle_round: out-of-place pending fold + product-grid lanes, host gruen_poly_from_evals). Dead worker's WIP = debug instrumentation, discarded. **stage5 @2^22 same-arm ablation: 4.26→2.53s (−41%), prove_round 3.15→1.33 (−58%)**; device prove_round pinned 1.33-1.38 across runs (co-load-insensitive — GPU-ALU immunity to page-cache pressure confirmed again). Exact-dispatch-count tests (30 full-device / 21 mid-sumcheck-handoff@gate-2^11). byte_diff 19/19. Cross-arm + e2e pairs junk in ambient (CPU legs swing ±50-70% — 16GB page-cache pressure; DorySetup 1.8↔6.9s!). Staying on @8f26853ac, orch cross-merges. Next: st6b.
- 19:43 ET: **Daemon deploy restart stopped W4b** (real stop, explained) — resumed same task_id, straight into st6b. W3a/W3b already merged, nothing else to revive.
- 18:32-18:55 ET: **529 STORM killed W4 AND the orchestrator.** Orchestrator respawned as task 88f47a13 — old subtree SEVERED (inspect/resume on 5a0187aa = access denied; no daemon auto-resume twin this time). W4's worktree intact: +1 landed commit 9d659d256 (device cycle-table materialization) + uncommitted WIP in slots/instruction_read_raf.rs. **W4b spawned (54fc22d6)** fresh in same worktree w/ full reconstruction prompt (read lane commits + journal checkpoint + assess WIP; st5 cycle rounds → st6b; base-vs-rebase delegated). ORCH LESSON: after any orchestrator 529 death, expect subtree severance — resume via fresh spawn w/ reconstruction prompt, and re-verify EVERY child by worktree/branch state (not task API).
- 18:20 ET: **W3b FINAL (P1+P2)** — **st8 @2^22: 7.85→3.33s (−58%)**; combine_hints 2.74→0.39 (~6×, thread-per-row high-to-low double-and-add — shared γ-power scalars make row-Horner beat Pippenger at N≤42: warp-uniform branches, zero threadgroup mem, raggedness = prefix break; hints sorted desc). jolt-dory seam = guard-scoped fn-pointer hook (install_combine_hints_hook), ProofSession-parked, uninstalls at proof end. VMP 1.26→0.26. e2e @2^22 paired 27.96→21.46 (incl W3a). @2^23 st8 projection ~4.5-5 (reduce-rounds growth + prepare decode = the gap to the 3.5-4 target). byte_diff 19/19 ±metal on rebased branch; 178/178. **Merged ff → trunk @7b96cf55b, 178/178 re-verified.**
- **CEILING EROSION FLAG (for milestone report, not piecemeal):** measured @2^22 e2e = 1.30× (W2+W3a+W3b); with W4's realistic adds → **~1.35-1.5× @2^23 projected final** — below the 1.7-2.2 band. Erosion = named floors accumulating: st0 tier-2 Miller lane 5.9-6.3, st8 dory-pcs reduce ~2.5-3 + prepare decode, st1-4 untouched 5.3, outside-stage 3.7. Unpulled levers: i128 MSM→device (−1 st0), st1-4 walk dedup (−1.5-2?), B1 cross-relation batching (big rewrite). Milestone report leads with the honest number + erosion ledger.
- 18:16 ET: **W4 checkpoint #1** @55aa04c17 — st5 IRR phase scans on device, parity-green: PhaseScanner seam (scan→shared assembly = structural parity) [8ef1f53d1, CPU-side refactor commit] + Metal slot (1 CB/phase, 4 dispatches, rows/u_evals/buckets zero-copy). Solved: Fr-scatter via per-SIMDGROUP bucket rows + simd_shuffle_xor butterfly (skew-robust), raw-space accumulate + one host ×R fixup (= CPU deferred accumulator, no R² in MSL), ALL 43 suffix_mle variants in MSL w/ u128 emulation (40k-case exhaustive probe), corrupt-recovery rebuilds u_evals (no ping-pong, 16GB-aware). muldiv metal arm green. Next: st5 cycle rounds → st6b.
- 18:16 ET: **W3b alive + Part 2 SHIPPED** @7b96cf55b (device combine_hints, shared-scalar G1 row combination) — 17:43 stop event was spurious (#5); silence was heads-down rebase+implement. Gates green 18:08; A/B bench pending load-settle (box load avg 32! three workers colliding — FINAL ladder must run on quiet box after all workers land).
- INCIDENTS: metal-w4 worktree DELETED under running worker ~17:45 (recovered, branch intact, papercut logged — repo now hosts 3+ concurrent campaigns: perf-dory-cpu-wins + tracer-100mhz worktrees appeared, main moved to 9e9028d51). perf-dory-cpu-wins likely = CPU-gift lane running elsewhere → final table pins baselines BY COMMIT.
- 17:42 ET: **W3b Part 1 shipped** @e10793ce1 (on pre-merge base — rebase pending): MetalJointOpening w/ build_opening_views extraction (CPU fallback = literally the optimized views), jk_opening_fold_{dense,onehot} thread-per-column zero-scatter, 5-6 family dispatches/1 CB = one pass over packed columns, i128→Fr lift on device (FR_R2 preamble), fold cache behind RlcSource. **VMP window 0.62→0.28s @2^22** (folds 0.59→0.03 device); st8 conservative 6.4→~6.0 (mass is EC, as mapped). Copies: 0 for 256MiB columns (plain Vec ≥32KB no-copy!), 1 planned (left vector), test-pinned. byte_diff 19/19 both modes on its base. Part 2 designed (CSR gather + 4-bit-window Jacobian buckets + host batch-normalize) — GO delivered 17:42:20 (crossed with its final; queued-delivery latency ~25min — watch this). W4 landmines relayed by W3b: repeatable dispatch-before-run batching; plain-Vec no-copy at ≥32KB; pre-commit clippy runs WITHOUT metal (dead-code trap for metal-only pub(crate)); ±40% same-code swings under co-run → pin changed component via in-trace windows; modular st8 is non-ZK-only (no y_com device work).
- 17:08 ET: **W3a FINAL** — 4 commits: 694d4dc89 shared-G2-prep [CPU-side, measured WALL-NEUTRAL @2^22 — traffic model's "42% of tier-2" receipt REFUTED at one_hot_k=16, Miller loops dominate; kept: byte-safe + metal lane rides its prep] · 5a0a1e02f g1.metal (Fq CIOS FQ_* preamble, Jacobian dbl-2009-l/madd-2007-bl, jk_g1_seg_sum, zero-copy &[G1Affine] views, PendingPass) · d792e619d Tier2Accumulator (order/partition-free Miller absorb, 1 final exp) · 981e15156 MetalCommitWitness 3-lane pipeline w/ TypeId (Fr,DoryScheme) install. **stage0 @2^22: 9.02→5.48s (−39%), E2E 25.92→22.65; @2^20: 3.51→2.27.** GPU lane (~1.3-1.7s) fully hidden under CPU Miller lane. @2^23 floor named: tier-2 rows/col FLAT across scales → CPU lane ≈5.9-6.3s; W4-lever: i128 MSMs to device (−~1s). Parity: unit + sharded-accumulator + commit+hints pinned vs optimized; muldiv 5/5.
- 17:1x ET: **Merged W3a → gpu/metal-backend @8f26853ac** (union conflicts in metal/{mod,runtime}.rs + one semantic fix: dory_commit_slot::<F,PCS> → ::<Fr,PCS> under W2's Fr-concrete impl). Gates on merged tree: clippy metal clean, jolt-kernels 173/173 metal, muldiv 5/5 both modes. W3b signaled: Part 2 GO (rebase + combine_hints MSM; tier2.rs extend-don't-restructure). NOTE: gpu-metal-backend worktree has an UNRELATED pre-existing stash (stack-fix-10) — not ours, left alone.
- 17:2x ET: **W4 spawned (5a0187aa)** on gpu/metal-w4-stages @8f26853ac: st5 IRR 46 phase scans → device streams (ROI #1) · st6b materialize+gather-once device-resident (#2) · alloc-churn pooling w/ CPU-side-labeled split (#3) · st1-4 walk dedup stretch. Framing: port STREAMS not math (latency-hiding, not FLOPs). Target st5+st6b 11.4→6-7.
- 16:49 ET: **W3b checkpoint #1** — st8 @2^22 split: dory-pcs reduce rounds 2.66s/41% (EXTERNAL crate — named floor, no vendoring) + combine_hints 2.19/34% (the prize, blocked on W3a g1.metal) + fold VMP only 0.59/9% (Part-1 ceiling thin at 2^22; bigger @2^23) + JointOpening::prepare 0.93 (trace decode, not GPU-shaped). Plan approved: cycle-major col=cycle&(2^σ−1) zero-scatter, 4-family fused CB w/ left-match cache, PageAlignedVec column collection. Steered: st8 target rehonested to 3.5-4.0 @2^23; Part 2 takes priority on W3a-merge signal; no speculative stub work. **Ceiling tracker drift: ~1.6-1.9× realistic / 2.2 stretch** (floors named: dory-pcs 2.66, pairing lane, prepare decode) — report at W3-merge ladder w/ 3-column table, not incrementally.
- 16:29 ET: **W2 COMPLETE** @69623cbc6 (5 commits): 4 slots on device (inc_claim_reduction, hamming_weight_CR via N-concat kernel, ram_hamming_booleanity Gruen-inner-sums, ram_raf_evaluation reusing table_pairs at N=1 — zero new MSL), byte_diff **19/19 both modes** w/ device-forced probe-verified arms, all gates green. Pattern canonicalized in src/metal/slots/mod.rs docs. Landmines for W3/W4: phased rounds need per-phase tables or host handoff; 31-buffer cap; MSL reserves `half`; probe claims from reference tier only. Flaky advisory LEAK flag = Metal XPC shutdown timing.
- 16:2x ET: **Merged W2 ff → gpu/metal-backend @69623cbc6.** Spot-checks: muldiv metal 5/5; @2^20 same-session smoke metal 10.74 vs optimized 9.91 (+0.83s metal — W3a ambient load + suspected one-time MSL-compile cost; W2 pools too small to show wins; quantify clean at W3a-merge ladder, consider MTLBinaryArchive if compile cost confirms).
- 16:3x ET: **W3b spawned (62e6deaa)** on gpu/metal-w3b-opening: Part 1 = st8 fold_rows 42-pass device streams + prove_batch RLC folds (independent, landable alone); Part 2 = combine_hints windowed-MSM AFTER W3a's g1.metal lands (design now, implement post-rebase). File-ownership fence vs W3a (jolt-dory streaming/tier2, commitment.rs, schemes.rs = W3a's). Target st8 6.06→≤3.5.
- 16:02 ET: **529-recovery DOUBLE-RESUME race**: daemon auto-resumed W3a as 6d21ea9c (invisible — no child event) 3min before my manual resume 818dcf99; both ran same worktree. Twin committed Commit A @694d4dc89 (shared G2 prep, [CPU-side] labeled, clippy+193 tests green) — 818dcf99 adopted it; I stopped the twin. Papercut logged. **Single writer restored: 818dcf99.** Next: B1 Fq+g1.metal, B2 Tier2Accumulator, B3 MetalCommitWitness driver.
- 15:56 ET: W3a DIED on API 529 (real failure, not the spurious-stop bug; uncommitted Phase-2 WIP intact: jolt-dory tier2.rs + streaming/commitment/schemes touches). Resumed as **818dcf99** w/ commit-early directive + benchmark-contention discipline (its 15:55 @2^22 run 33.14s was polluted by W2 builds; baseline 20.3 — A/B pairs back-to-back, orchestrator quantifies clean at merge).
- 15:48 ET: **W2 checkpoint** @f5666cdf3 — slot pattern PROVEN: MetalIncClaimReduction, 1 fused dispatch/round (bind 4 tables ping-pong + tree-reduce partials, 1 CB), shared build_inc_tables w/ optimized twin (no math duplication), per-round tail re-gate, fail-mid-proof→CPU-finish (cur never corrupted), 16/16 rounds byte-equal @2^16 w/ ZERO copies, device 18.4ms vs CPU 20.9 even at tiny pool. OwnedDeviceBuffer added. probe/copied counters shipped. DEVIATION (accepted): JoltBackend::metal() now Fr-concrete. Next: hamming_weight → ram_hamming_booleanity → byte_diff arms. Merge note: W3a's (Fr,DoryScheme) TypeId guard remains compatible.
- 15:40 ET: **W3a checkpoint #1** — stage0 @2^22 measured: 7.79s = streaming 4.07 (98% G1: tier-1 35.3 thread-s = bucket-by-k then batch-affine-sum vs SRS bases; i128 4.6) + finish 3.72 (tier-2 per-column multi_pair 2^15 rows, host-locked). Structural: row points FINAL per window + Fp12 Miller products commute → incremental per-superchunk tier-2 is byte-identical; 40 columns pair against SAME g2 bases (shared prep = 30-45% tier-2 cut, CPU-side); GPU needs only group equality but will batch-normalize (Z=1) for coordinate-identical hints. OFFLOAD: device = tier-1 only (Fq CIOS + Jacobian, thread-per-segment gather w/ skew splitting); CPU = extraction/bucketing + i128 (unchanged) + incremental tier-2; 3-deep pipeline (extract s+1 ∥ GPU s ∥ Miller s−1). i128 Pippenger kernel REJECTED this wave (8.3 thread-s, wrong ROI). Projection: st0 @2^23 ≈ 4.5-5.5s; named floor if G2-sharing underdelivers: 5.8-6.3 (tier-2 = irreducible host lane). ORCH STEER: G2-shared-prep → separate commit wired into BOTH paths (cpu fix, quantified @2^22 before/after); Tier2Accumulator stays metal-lane; final table = optimized-orig vs optimized+cpu-fix vs metal.
- 15:03 ET: **W1 checkpoint** — runtime+field core DONE, 11/11 parity first-run green. objc2-metal 0.3.2 zero friction (protocols already Send+Sync, OnceLock context clean). CIOS mont_mul w/ ulong intermediates + branchless add/sub + bind + fused bind_eval (runtime eval-point set, threadgroup tree reduction). Constants from MontgomeryConstants seam. wrap_slice_nocopy w/ malloc-large gate + PageAlignedVec green on zero-copy round-trip. metal_gate env convention in (placeholder 2^17). Remaining: --backend metal arm, D1-D4 microbench.

## Pika-infra footgun (active)
Spurious "Stopped by user" child events (W1 15:04, W3a 15:41) — daemon bug (mis-scoped cleanup on unrelated-task steers, filed to infra audit). Sessions resurrect and keep working. Protocol: `pika-cli tasks inspect <id>` BEFORE reacting; only commit-preserve + resume if genuinely dead (last activity stale). Papercut logged.

## macOS footguns
- NO flock(1) binary on macOS → shell serialization = mkdir spin-lock (/tmp/jolt-gpu-lock.d); Rust tests = libc::flock(2) helper (told W1).
- newBufferWithBytesNoCopy: 16KB page alignment + page-multiple length; malloc-large (≥32KB) allocs are vm_allocate'd page-aligned → runtime check + copy fallback.
- ls/du aliased (eza/dust) in interactive shell — use /bin/ls, /usr/bin/du in scripts.

## Status log
- 14:20 ET Jul 29: campaign start. Journals read, branch gpu/metal-backend + worktree .worktrees/gpu-metal-backend created @bf802c135. Metal smoke PASS. Baseline build started (perf-kernels-optimized worktree, warm cache). Machine idle (only this orchestrator).
- 14:29 ET: Fractalyze memo landed + absorbed. Explore recon dispatched (seam specifics for worker prompts).

## Cross-campaign note (fp128 feasibility campaign, 15:05 ET Jul 29)
- Sibling campaign (bg 847961bf, journal .fp128-campaign-journal.md) running 128-bit-field (akita fp128, p=2^128−275) + 128-bit-sort GPU feasibility microbenches on THIS mini: standalone crate ~/dev/fp128-gpu-bench, 2 fable workers, NOT touching jolt worktrees.
- They honor /tmp/jolt-gpu-lock.d for ALL timed runs (GPU + CPU-baseline) — expect short holds (seconds-minutes, microbench scale). Reusing W1's runtime-MSL harness PATTERN from .worktrees/metal-w1 (read-only).
- Their int32-mul-roof / mulhi(ulong) numbers will be shared here when landed — directly useful for your mont_mul ceiling v2.

## fp128-campaign roof numbers (shared per cross-campaign note; measured 15:xx-16:00 ET, kernel-only timing, under /tmp/jolt-gpu-lock.d)
- M4 GPU integer roofs: mul32+add 500 G op/s · mulhi32+add 250 · mul64lo+add 124 · mulhi64+add 41 (64-bit mulhi emulation is catastrophic — keep BN254 CIOS on 32-bit limbs). fp32 FMA 2.34 TFLOPS sustained chained (ILP-8 flat).
- CAUTION for your ceiling v2: our GPU copy/triad measured only ~78.5-79.8 GB/s (vs your 96 solo) — likely YOUR side's unlocked cargo builds were drawing bus during our locked window. Locks only serialize bench-vs-bench; builds still pollute. Being re-verified at quiet; if your mont_mul microbench sees low GB/s, same cause.
- CPU fp128 (Solinas 2^128−275, akita asm path) for scale: ALU roof ~2.4 G mul/s @10C chained-ILP4; streaming bus-bound 1.9-2.1 G/s @93-103 GB/s. BN254 mont_mul roofline from mul32/mulhi32 roofs: ~64 mul32 + ~56 mulhi32 per CIOS mul ⇒ ALU cap ≈ min(500/64, 250/56) ≈ 4-7 G/s theoretical, expect 1-2.5 G/s real after latency/regs — your 0.8-1.5 est is consistent.

## CPU-tier gifts update (2026-07-29, perf-dory-cpu-wins lane — not metal)
- G2 prepared-cache priming SHIPPED: PR #1716 (branch perf-dory-cpu-wins, stacked on perf/kernels-optimized). Generation-guarded (first size primes, second size invalidates+disables). Measured −24%/multi_pair_g2_setup call @1024 pairs pure-modular; byte_diff 19/19. Metal baselines vs modular_benchmark unaffected — that harness was ALREADY cached (legacy preprocessing primes via DoryGlobals::init_prepared_cache, cfg(not(test))), so the roofline's multi_pair numbers did NOT include G2 re-prep waste.
- combine_hints GLV is a DEAD END: fork's G1 Mul is already GLV (GLVConfig::glv_mul_projective) — "1.25G plain double-and-add" premise wrong. GLV vector-op restructure benched ~15% slower; reverted. St8's combine_hints 1.25G figure = already-GLV work; W3b device MSM remains the only lever there.
- fp128-campaign CORRECTION to the note above: the "GPU copy/triad ~80 GB/s" caution was co-load contention, not a GPU-side ceiling — quiet-window remeasure: GPU copy 100.2 / triad 98.2 GB/s ≈ CPU streaming 93-106. "Fixed ~95 GB/s shared pie" model CONFIRMED from the GPU side. Useful for your ceiling v2: GPU ALU rates are co-load-insensitive (≤1% delta at loadavg 47) — only streaming shapes feel the pie. Final fp128 anchors: sol-4×32 fp128 mul 11.3 G/s on-GPU (95% of the 501 G mul32 issue roof, 42 mul-ops/mul); fp128 streaming parity 0.98-1.07× vs CPU. Full tables: ~/dev/fp128-gpu-bench/RESULTS.md.

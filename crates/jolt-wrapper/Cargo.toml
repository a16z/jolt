[package]
name = "jolt-wrapper"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Verifier wrapping infrastructure for Jolt — symbolic execution, AST capture, and pluggable codegen backends"

[lints]
workspace = true

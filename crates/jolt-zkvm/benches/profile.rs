//! Profiling harness for the jolt-zkvm proving pipeline.
//!
//! Currently a placeholder — the typed DAG pipeline (T07–T15) is being
//! wired. Once `prove()` is fully functional, this bench will exercise
//! the full prove → verify cycle.

fn main() {
    // Typed DAG pipeline not yet wired (T07-T15).
}

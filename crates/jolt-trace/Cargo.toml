[package]
name = "jolt-trace"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Tracer-free instruction and cycle traits for Jolt"
repository = "https://github.com/a16z/jolt"
keywords = ["SNARK", "zkvm", "risc-v", "trace"]
categories = ["cryptography"]

[lints]
workspace = true

[features]
test-utils = []

[dependencies]
jolt-riscv = { workspace = true }
rand = { workspace = true }

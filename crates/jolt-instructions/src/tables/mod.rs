//! Lookup table definitions for Jolt instruction decomposition.
//!
//! This module re-exports the [`LookupTable`](crate::LookupTable) trait.
//! Concrete table implementations will be added when the prover pipeline
//! is integrated in the `jolt-zkvm` crate.

// Re-export for convenience; actual table structs will live here.

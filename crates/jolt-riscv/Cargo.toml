[package]
name = "jolt-riscv"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "RISC-V instruction kinds and circuit/instruction flag types for Jolt"
repository = "https://github.com/a16z/jolt"
keywords = ["SNARK", "zkvm", "risc-v"]
categories = ["cryptography"]

[lints]
workspace = true

[dependencies]
rand = { workspace = true }
serde.workspace = true
strum = { workspace = true, features = ["derive"] }

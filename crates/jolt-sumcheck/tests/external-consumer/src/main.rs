// Isolated from workspace feature unification; keep the source identical to
// the public example rather than maintaining a second copy.
include!(concat!(
    env!("CARGO_MANIFEST_DIR"),
    "/../../examples/clear_custom_transcript.rs"
));
